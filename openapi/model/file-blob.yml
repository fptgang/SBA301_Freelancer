FileBlobDto:
  type: object
  properties:
    uploaderId:
      type: integer
      format: int64
    blob:
      type: string
      format: binary
    isVisible:
      type: boolean
    messageId:
      type: integer
      format: int64
    proposalId:
      type: integer
      format: int64
    projectId:
      type: integer
      format: int64
