MessageDto:
  type: object
  properties:
    messageId:
      type: integer
      format: int64
    senderId:
      type: integer
      format: int64
    projectId:
      type: integer
      format: int64
    content:
      type: string
    isVisible:
      type: boolean
    files:
      type: array
      items:
        $ref: 'file.yml#/FileDto'
    createdAt:
      type: string
      format: date-time