FileDto:
  type: object
  properties:
    uploaderId:
      type: integer
      format: int64
    fileId:
      type: integer
      format: int64
    fileName:
      type: string
    fileUrl:
      type: string
    fileType:
      type: string
    size:
      type: integer
      format: int64
    isVisible:
      type: boolean
    createdAt:
      type: string
      format: date-time
    messageId:
      type: integer
      format: int64
    proposalId:
      type: integer
      format: int64
    projectId:
      type: integer
      format: int64
    milestoneId:
      type: integer
      format: int64
    description:
      type: string