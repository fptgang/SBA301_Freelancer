FileDto:
  type: object
  properties:
    fileId:
      type: integer
      format: int64
    uploader:
      $ref: 'account.yml#/AccountDto'
    fileName:
      type: string
    fileUrl:
      type: string
    fileType:
      type: string
    size:
      type: integer
      format: int64
    isVisible:
      type: boolean
    createdAt:
      type: string
      format: date-time
    messageId:
      type: integer
      format: int64
    proposalId:
      type: integer
      format: int64
    projectId:
      type: integer
      format: int64
    milestoneId:
      type: integer
      format: int64
