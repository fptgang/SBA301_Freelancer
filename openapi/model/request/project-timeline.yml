ProjectTimelineDto:
  type: object
  properties:
    newStartDate:
      type: string
      format: date-time
    milestones:
      type: array
      items:
        type: object
        properties:
          milestoneId:
            type: integer
            format: int64
          newDeadline:
            type: string
            format: date-time
