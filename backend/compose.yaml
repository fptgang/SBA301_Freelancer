name: hirable

services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=hirable'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=myuser'
    ports:
      - '3307:3306'
    networks:
      - app-network
  redis:
    image: 'redis:latest'
    ports:
      - '6380:6379'

networks:
  app-network:
    driver: bridge